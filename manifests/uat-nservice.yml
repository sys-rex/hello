apiVersion: v1
kind: Service
metadata:
  name: py-app
  namespace: uat-ns
  labels: 
    app: py-one
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: py-one
  type: LoadBalancer

